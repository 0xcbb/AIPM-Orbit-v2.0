---
产品: "[[CP17_网络收音机]]"
类型: 测试记录
状态: 部分通过
版本: V2.0.0-dev
created: 2026-07-13
updated: 2026-07-15
---
# V2.0 测试记录

## 自动化

- 通过：私人连接模型、局域网 HTTP / 公网 HTTPS 门禁、配置持久化、WebDAV XML 解析、Basic Auth 请求、音频 / LRC / 封面关联和私人曲目不进入普通电台快照。
- 通过：SMB 私网 / Tailscale 地址门禁、递归目录扫描、同目录歌词与封面关联、本机随机令牌 Range 响应和播放 URL 凭据防泄漏。
- 通过：`flutter test --no-pub`，57 / 57。
- 通过：`flutter analyze --no-pub`，0 issue。
- 通过：`flutter build apk --debug --no-pub`。
- 通过：APK 内包含 `arm64-v8a`、`armeabi-v7a`、`x86_64` 三套 `libsmb2.so`。

## 真机

- 通过：moto G55 `XT2435-3 / Android 16 API 36 / ZY22KLPKXJ` 覆盖安装，版本 `2.0.0 (202)`。
- 通过：普通空间固定坐标脚本启动；设置入口、私人微电台空态、WebDAV / AList / NAS 分段和 SMB 添加表单均可访问，横屏无 Flutter overflow。
- 通过：导航热修 `ca94c4f`、构建提交 `981f200` 覆盖安装；按“首页 -> 设置 -> 私人微电台 -> 左边缘返回”操作后恢复原设置面板，不再越级返回首页。
- 通过：地图热修 `89978b3` 覆盖安装；地图全宽 provider 来源栏已移除，标题副标题保留轻量来源，V2 普通空间竖屏 smoke 通过。
- 待执行：真实 WebDAV / AList WebDAV 认证、保存重启、扫描、点播、删除、后台与蓝牙切歌。
- 待执行：真实群晖 / QNAP SMB2/3 账号下的扫描、点播、进度拖动、断线恢复、后台 30 分钟、网络切换和蓝牙切歌。

## 结论

- 当前为 V2.0-dev 可安装开发包，不是 release candidate。
- 自动化、APK 原生库检查与入口 UI 已通过；没有真实服务账号，因此端到端远端能力仍为 **待人工确认**。
- `dart_smb2` 为 BSD-3-Clause，随包动态链接的 `libsmb2` 为 LGPL-2.1-or-later；商业发布前的源码 / relinking offer 与商店披露方式为 **待人工确认**。
